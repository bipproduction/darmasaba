import { default as userRole } from "./userRole.json";
import { default as users } from "./users.json";
const jsonData = {
  userRole,
  users,
};
export { userRole, users };
export default jsonData;
