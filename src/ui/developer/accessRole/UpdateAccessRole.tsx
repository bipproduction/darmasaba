'use client'
import { Card, Stack, Title } from "@mantine/core";

export function UpdateAccessRole() {
    return <Card withBorder>
        <Stack>
            <Title order={4}>Update Access Role</Title>
        </Stack>
    </Card>
}